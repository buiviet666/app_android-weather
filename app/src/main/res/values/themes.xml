<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Weather_app" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- primary brand color -->
        <item name="colorPrimary">@color/purple_200</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- second brand color -->
        <item name="colorSecondary">@color/steal_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- status bar color -->
<!--        <item name="android:statusBarColor" tools:targetApi="l">?attr/color/colorPrimaryVariant</item>-->
    </style>

    <style name="Theme.Weather_app" parent="Base.Theme.Weather_app" />
</resources>